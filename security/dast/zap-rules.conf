# OWASP ZAP baseline scan rule configuration
# 10096 = Timestamp disclosure (INFO) — noisy in dev
# 10027 = Information disclosure in URL (INFO)
# 90022 = Application Error Disclosure (WARN) — keep enabled
# 10021 = X-Content-Type-Options Header Missing (WARN) — we fix this
# 10020 = X-Frame-Options Header Missing (WARN) — we fix this
# 10035 = Strict-Transport-Security Header Missing (WARN)
# 10038 = Content Security Policy Header Missing (WARN)

10096	IGNORE	(Timestamp Disclosure)
10027	IGNORE	(Information Disclosure - Suspicious URL)
10049	IGNORE	(Storable and Cacheable Content)
10109	IGNORE	(Modern Web Application)
